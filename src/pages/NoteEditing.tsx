import React from "react";
import SinglePage from "src/components/ui/layout/SinglePage";

const NoteEditing = () => {
    return (
        <SinglePage>
            <div>Note editing page</div>
        </SinglePage>
    );
};

export default NoteEditing;
