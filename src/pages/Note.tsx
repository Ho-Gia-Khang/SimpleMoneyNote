import React from "react";
import SinglePage from "src/components/ui/layout/SinglePage";

const Note = () => {
    return (
        <SinglePage>
            <div>Note Page</div>
        </SinglePage>
    );
};

export default Note;
